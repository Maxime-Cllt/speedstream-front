services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - APP_MODE=${APP_PORT-PROD}
      - API_URL=${API_URL-http://localhost:8080}
      - API_TOKEN=${API_TOKEN}

    env_file:
      - .env